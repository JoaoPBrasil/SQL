SELECT COUNT (*)
FROM tabela;

SELECT COUNT (DISTINCT coluna1)
FROM tabela;

-- Desafio 1:

SELECT COUNT(*)
FROM Production.Product;

-- Desafio 2:

SELECT COUNT(Size)
FROM Production.Product;

-- Desafio 3:

SELECT COUNT(DISTINCT Size)
FROM Production.Product;
